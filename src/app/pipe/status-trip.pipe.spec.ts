import { StatusTripPipe } from './status-trip.pipe';

describe('StatusTripPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusTripPipe();
    expect(pipe).toBeTruthy();
  });
});
