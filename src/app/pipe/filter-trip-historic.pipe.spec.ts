import { FilterTripHistoricPipe } from './filter-trip-historic.pipe';

describe('FilterTripHistoricPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTripHistoricPipe();
    expect(pipe).toBeTruthy();
  });
});
